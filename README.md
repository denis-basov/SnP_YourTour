# SnP_YourTour

Как запускать:

Одиночная сборка (разовая компиляция):
npm run sass:build

Режим наблюдения (автопересборка при изменениях):
npm run sass:watch

После этого при изменении файлов в SASS/ автоматически будет обновляться styles/main.css, который уже подключен в index.html.